@model CMS.Data.Entities.CategoryProduct

@{
    ViewData["Title"] = "Xóa danh mục sản phẩm";
}

<div class="container mt-4">
    <h2 class="text-danger text-uppercase">XÓA DANH MỤC SẢN PHẨM</h2>
    <p>Sinh viên thực hiện: <b>Nguyễn Định Nam - 2123110170</b></p>

    <div class="alert alert-warning">
        <h4>Bạn có chắc chắn muốn xóa danh mục này không?</h4>
        <p>Lưu ý: Nếu xóa danh mục này, các sản phẩm thuộc danh mục này có thể bị ảnh hưởng!</p>
    </div>
    <hr />

    <div>
        <dl class="row">
            <dt class="col-sm-3 font-weight-bold">Tên danh mục sản phẩm:</dt>
            <dd class="col-sm-9">@Model.Name</dd>
            @if (!string.IsNullOrEmpty(Model.Description))
            {
                <dt class="col-sm-3 font-weight-bold">Mô tả:</dt>
                <dd class="col-sm-9">@Model.Description</dd>
            }
        </dl>

        <form asp-action="Delete" method="post">
            <input type="hidden" asp-for="Id" />
            <button type="submit" class="btn btn-danger">Xác nhận Xóa</button>
            <a asp-action="Index" class="btn btn-secondary">Hủy và quay lại</a>
        </form>
    </div>
</div>
